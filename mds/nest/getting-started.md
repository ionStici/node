# NestJS CLI

```bash
# Install Nest CLI globally
npm install -g @nestjs/cli

# Check if installed
nest --version

# Look for available commands
nest --help

# Create a new Nest app
nest new nest-app

# Start the dev env
npm run start:dev
```
